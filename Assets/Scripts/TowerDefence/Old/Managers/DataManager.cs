using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Manages the level data
/// </summary>
public class DataManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//download version.json to check currentverion against current downloaded it it exists.
        //if version differs download new level.json
	}
}
